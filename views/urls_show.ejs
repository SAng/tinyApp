<!DOCTYPE html>
<html lang="en">
  <head>
    <% include partials/_header %>
  </head>
  <body>
        <!-- Add your EJS code here -->
    <h1>Shortened URL and URL</h1>

    <%= shortURL + ': ' + ((urls[shortURL]) || 'Invalid Shortened URL') %>

    <form action="/urls/<%= shortURL %>/update" method="POST" style="margin:150px;">
      <label for="newlongURL">Enter a new URL:</label>
      <input id="newlongURL" type="text" name="newlongURL" placeholder="http://" style="width: 300px">
      <input type="submit" value="Update">
    </form>

  </body>
</html>